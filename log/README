最初に、コマンドコンソールで、


  // スレッドをコンソールから作成
　generate_thread(1, "first message");
  generate_thread(2, "second_message");
  generate_thread(3, "third_message");
  
を作成。

動かない場合は、コマンドコンソールの「ストレージ」で localstorage_data.png の内容を保存
