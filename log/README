Mac で 動きます。

最初に「Control + m」&「スレッド追加」で動かない場合は、

コマンドコンソールの「ストレージ」の内容を全部消した後で、
コマンドコンソールで以下の内容を打ち込みます。

  // スレッドをコンソールから作成
　start_thread();
  start_thread();
  start_thread();
  
を作成。

動かない場合は、コマンドコンソールの「ストレージ」で localstorage_data.png の内容を保存
